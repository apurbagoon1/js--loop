// VARIABLE DECLARATIONS
const price = 350; // Constant value that won't change
let quantity = 5; // Variable value that can change

// CONDITIONAL STATEMENT
if (price > 200) {
    console.log('That’s a bit expensive!');
} else {
    console.log('Great deal!');
}

// ARRAY OF NUMBERS
const values = [18, 42, 57, 69];
console.log('Values:', values);
